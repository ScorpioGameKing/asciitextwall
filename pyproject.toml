[project]
name = "asciitextwall"
version = "1.0.0"
description = "Simple tool to generate ascii text wallpapers"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["pillow>=12.2.0", "pyfiglet>=1.0.4"]

[tool.pylint."messages control"]
disable = ["missing-function-docstring"]
